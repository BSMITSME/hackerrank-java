첫번째 시도방법
1. ranked 배열에 player 값을 하나씩 넣어주며 정렬 및 Map에 작성
2. ranked와 Map도 값이 들어올때마다 새로 갱신 -> 답을 도출하는 과정이 값이 들어올 당시 순위를 산출
3. timelimit 문제 발생

최적화 필요
순서 보장, 인덱싱(위치 접근 가능) -> 트리 알고리즘? heap 정렬 고려 해보기  

- heap 으로 순서를 보장받는다면 인덱스를 어떻게 접근할 것인지 생각


